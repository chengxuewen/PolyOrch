# PolyOrch — Status

> Loaded into `instructions[]` on every turn. Keep it short — detail belongs in `decisions.md` / `pitfalls.md`.

## Project

| Field | Value |
|---|---|
| Name | PolyOrch |
| Path | `DEVSYS/PolyOrch` |
| Domain | Polyglot monorepo build orchestration |
| Brand / CLI | `PolyOrch` / `polyorch` |
| Official acronym | **PBOS** (Polyglot Build Orchestration System) |
| Stack | PolyOrch itself: **CMake helper surface** (D16, supersedes D3 delivery form). Environment: Pixi. xmake = reference corpus + package-management source (vcpkg · Conan via Xrepo, not bridges) |
| Vendored deps | `.agents/skills/xmake-*` / `xrepo-*` -- 58 Xmake agent skills (Apache-2.0, pinned commit `ef67caa`). See `XMAKE-ATTRIBUTION.md` |
| Language policy | **English for all artifacts.** Chinese only for AI chat and plan docs under `.omo/` — see C4 |
| Git | `main`; docs-first history superseded -- `cmake/`, `tests/`, `examples/`, `docs/` all carry committed substance since 2026-09-21 |

> Working-copy location is intentionally not stated in this file (C6); see D10 and the git-excluded plan zone.

## Phase

`P2 — design landed`. Whitepaper v1.0, 12 verified project profiles, the architecture design baseline (`docs/architecture.md`), and the module reference (`docs/modules/`) are in place. A CMake helper surface has landed in-tree (`cmake/`, `scripts/`, `tests/`, `examples/`); the D3 product form (Lua in an Xmake addon) has NOT yet consumed it -- see Open Items.

## Modules

| Module | What |
|---|---|
| `cmake/PolyOrchPixiHelpers.cmake` | single pixi module (~1500 lines, D12): find / tool_ensure / tool_install / install / env_target / env_paths / activate_script / scripts_install / setup / report / bootstrap + manifest-mutating actions |
| `cmake/PolyOrchOptionHelpers.cmake` | `polyorch_option` + expression helpers |
| `cmake/PolyOrchFindRust.cmake` | rust toolchain layer (D16 split): setup(FROM system\|pixi) + EXECUTABLE injection, WP3 parity surface (rustup proxy tell + enumeration, `PolyOrch_RUST_TOOLCHAIN` knob with `rustup show` host expansion, `polyorch_rust_version_ok` + `PolyOrch_RUST_MIN_VERSION` floor, `PolyOrchRust::{Rustc,Cargo}` imported handles, `_polyorch_rust_derive_target` seam), triple families, artifact naming (WP5 `TARGET` = .cargo-target/<tup>/ cross nesting), native-static-libs probe on the EFFECTIVE triple, cargo command wrapper (PATH + host-env isolation, PIT-14; WP5 strip-before-forward ordering contract); WP4 pure staging helpers (`_polyorch_rust_copy_plan` `TARGET`/`PROFILE` extension incl. gnullvm deps/, `_polyorch_rust_sanitized_out_dir`); WP5/WP5b pure env builders (`_polyorch_rust_triple_env_form`, `_polyorch_rust_forward_env` cc-rs trio + Apple block, `_polyorch_rust_linker_plan` linker/runner conditions); setup CONSUMES `PolyOrch_RUST_CARGO_TARGET` (derive chain + pre-discovery family gate + host-equal normalization); WP7 tool layer: `polyorch_rust_tool_bootstrap` (discovery-first, ALLOW_INSTALL+lock-compare gated deferred cargo-install rule via the command wrapper, `<TOOL>_TOOL` FILEPATH cache, script-mode rule guard) + pure `_polyorch_rust_tool_version_check` + `_polyorch_rust_cxx_version_required` (cargo-tree probe) |
| `cmake/PolyOrchRustHelpers.cmake` | rust build-graph API (D13/D14): build / import(metadata) / 4 setters / test / run / clean / install(+export stub), mediator + property carrier, FOLDER, collision guard (PIT-13); WP4 MC profile genex (corr:762/772) + deferred finalize: late per-CFG `IMPORTED_LOCATION(_<CFG>)`/`IMPORTED_IMPLIB(_<CFG>)`, output-dir staging copies, corr:2313 property init (D17); WP5 cross routing: `--target` genex, UNSTAMPED cross mediators, hostbuild late layer, cc-rs dual-trio env, `polyorch_rust_set_hostbuild`; WP5b linker plane assembly (`_polyorch_rust_linker_entries`: explicit/cache knob > wrapper file > default cc/c++ pick, RUNNER forwarding, run() emulator prefix) + `polyorch_rust_link_libraries` (staticlib -> interface, target -> -L/-l genex); WP6 parity cluster: `polyorch_rust_package_version` (+ pure `_polyorch_rust_metadata_package_version`; `POLYORCH_RUST_PKG_<name>_VERSION` cache), import-time global defaults (`PolyOrch_RUST_ALL_FEATURES`/`NO_DEFAULT_FEATURES`/`CARGO_FLAGS`/`VERBOSE`/`NO_USES_TERMINAL`, corr:690-700 shape, property-seeded so the guarded genex chain is unchanged), default kind pair (`PolyOrch_RUST_DEFAULT_KINDS`, kind-less dispatch to `-static`/`-shared`/`-exe` handles), USES_TERMINAL default on cargo rules via `_polyorch_rust_uterm`; WP8 install surface: `_polyorch_rust_install_plan` pure row planner (destinations x permissions x component x configurations x stub-entry rows, table-locked), `polyorch_rust_install` keyword expansion `RUNTIME_/ARCHIVE_/LIBRARY_DESTINATION` + flat `PERMISSIONS` + `COMPONENT` (extension, no upstream mechanism) + `CONFIGURATIONS` gating + `PUBLIC_HEADER` (flat include/ sidecar + stub `INTERFACE_INCLUDE_DIRECTORIES` line), generated `<export>Config.cmake` find_package wrapper beside the replay stub (`PACKAGE_PREFIX_DIR` set inside it but swallowed by CMake at the find_package boundary, measured 4.4.3 -- the `include()` is the load-bearing line); MC/per-CONFIG stub staging seam-documented; WP7 tool clusters: `polyorch_rust_cxxbridge` (generated-tree layout, builtin-header + per-file rules via pure `_polyorch_rust_cxxbridge_cmd`, pair link edges, REGEN_TARGET, touch-rerun DEPENDS) + `polyorch_rust_cbindgen` (dual signature per pin, bare `-E env TARGET/CARGO/RUSTC` rule via pure `_polyorch_rust_cbindgen_cmd`, DEPFILE rerun, regen targets + mediator edge) + `POLYORCH_RUST_MANIFEST` handle stamp in build() |
| `scripts/` | activation trio `pixi.sh` / `pixi.bat` / `pixi.ps1`; installed beside `.pixi/` via `COPY_SCRIPTS` |
| `examples/` | pixi-bootstrap (`cmake -P`), pixi-configure, pixi-workspace; `rust-basic` -- system rust route, no pixi and no network since WP2; each also a `PolyOrchExample*` target |
| `tests/` | `run.sh` driver + CTest registration; 56 `cmake -P` cases; shared marker contract incl. `# requires: <cap>` capability gates (cases/_requires.cmake; `system-rust` probes PATH + `~/.cargo/bin` since WP2, aligning the parent gate with the driver-composed child PATH), contract-SKIP veto and driver tripwires (D15) |

## Verification

| Check | Command | Result |
|---|---|---|
| opencode config parses | `python3 -m json.tool .opencode/opencode.json > /dev/null` | pass (2026-09-18) |
| no domain leakage into toolchain | `grep -rliE ... .agents/rules .agents/skills .opencode .gitignore` (drop `.omo` -- it is the git-excluded Chinese plan zone; a 2026-09-20 verdict doc legitimately mentions the old domain there) | empty on the committed surface (2026-09-21) |
| brand / naming layering | see `conventions.md` C1 | pass (2026-09-18) |
| canonical taglines byte-exact | see `conventions.md` C2 | pass (2026-09-18) |
| docs cross-links resolve | see `conventions.md` C3 | pass (2026-09-18) |
| derived-doc set classified | see `conventions.md` C2 | pass (2026-09-20) |
| English-only artifacts | see `conventions.md` C4 | pass (2026-09-18) |
| vendored Xmake skills unmodified | see `conventions.md` C5 | pass (2026-09-20) |
| design-doc separation (no cross-naming) | see `conventions.md` C6 | pass (2026-09-20) |
| cmake unit suite | `bash tests/run.sh` | pass=44 fail=0 skip=12 (12 e2e driver-gated) offline; **54 pass / 0 fail / 2 skip** with `POLYORCH_TEST_E2E=1` -- the sole fail is the `t-rust-e2e` pixi keeper: `tls handshake eof` from conda-forge during the fixture's pixi solve (the R-12 state, retried 4x on 2026-09-22; every non-keeper leg incl. the WP6 cluster and both new WP8 cases green, zero other skips); (2026-09-22, WP7: +`t-rust-toolplan` -- version lock-compare tables + bootstrap discovery/deferral/script-mode-install-guard legs; +`t-rust-cxxbridge` / +`t-rust-cbindgen` -- argv tables + STUB-tool rule legs (fixtures/cxxbridge, fixtures/cbindgen, fixtures/tool-stubs; discovery+lock gate, generated tree layout, real C++ compile of generated sources, functional -E env asserts through the stub's echoed header, touch-rerun DEPENDS edge, signature guards); the two live legs +`t-rust-cxxbridge-e2e`/`t-rust-cbindgen-e2e` CONTRACT-SKIP cleanly (exact lines recorded in the WP7 report) pending `# requires: cxxbridge-cmd`/`cbindgen` + crates.io + the cargo-install authorization; WP8: +`t-rust-installplan` -- pure planner row tables (kinds x destination overrides x prefix folding x permission override x component/configurations stamping x header rows x implib basename) + 3 guard pins; +`t-rust-install-export` -- full-shape install (custom RUNTIME dest, one COMPONENT, PUBLIC_HEADER) with layout/exec-bit asserts, per-component `--install --component` filtering both ways, a find_package(CONFIG) consumer that COMPILES against the staged header, RUNS through the staged .a, and the stripped-stub `pow` negative leg -- existing `t-rust-install-e2e` passes UNEDITED through the planner path; earlier WP6: +`t-rust-knobs` -- global VERBOSE/ALL/NO_DEFAULT/CARGO_FLAGS tokens in generated build.make, setter non-leak leg, Ninja `pool = console` USES_TERMINAL-polarity halves; +`t-rust-pkgver` -- canned-JSON pure tables + real-metadata legs on the import-ws fixture behind `# requires: system-rust`; +`t-rust-defaultkinds` -- kind-less STATIC;SHARED dispatch, BIN knob leg with `--bin` rule text, opt-out FATAL, mediator-clash loud failure; earlier WP5/WP5b: +`t-rust-crossplan` -- TARGET naming/copy tables, cc-rs trio gate, generated --target/hostbuild/order/no-op legs; +`t-rust-linkplan` -- linker-plan tables + explicit/wrapper/default/CXX/sysroot-gate/run-prefix children; +`t-rust-musl` real cross e2e; `t-rust-executables` CARGO_TARGET leg flipped echo->consumed/gated per the WP5 contract) |
| CTest registration | throwaway host x `-DPolyOrch_TEST_E2E=ON` + `ctest --test-dir <b>` | full ctest leg green in all 4 matrix cells (2026-09-22); **blocked standalone** -- root configure FATALs at PlatformSupport mkspec detection without host context (c79c4bf known state) -- re-run under the host or after mkspec fallback lands |
| rust offline suite | `bash tests/run.sh` | rust cases 29/29 pass + 11 e2e-skip (2026-09-22, WP6: +knobs/+pkgver/+defaultkinds; WP8: +installplan; WP7: +toolplan/+cxxbridge/+cbindgen offline legs, +cxxbridge-e2e/+cbindgen-e2e gated) |
| rust e2e (system cargo) | `POLYORCH_TEST_E2E=1 bash tests/run.sh` | the seven legs rule-wiring / import-ws / link-c / install-e2e / install-export / profile-release / output-dir EXECUTE on the system toolchain behind `# requires: system-rust` (the driver composes the child PATH with `~/.cargo/bin`; profile-release composes it itself); since WP3 these children resolve through the rustup PROXY at `~/.cargo/bin` down to the concrete toolchain -- the proxy branch proven under real rustup 1.29.1. since WP5 the musl leg `t-rust-musl` executes the REAL cross route (rustup target x86_64-unknown-linux-musl: static bin + archive asserted by ELF magic + file(1), hostbuild fall-back to the distinct host dir; the static-PIE reality of rust 1.98 musl linking is documented at the case); `t-rust-link-c` additionally runs the WP5b `polyorch_rust_link_libraries` direction (Rust bin <- CMake C staticlib, marker `POLYORCH_LINKLIB_OK 7`). `t-rust-e2e` remains the single `# requires: pixi-rust` keeper for the FROM pixi route (env materialized every run, R-12 exposure confined to it -- it flaked once during WP3 and passed on re-run for 41/41) AND doubles as the live NON-PROXY proof: conda's rustc answers the `RUSTUP_FORCE_ARG0=rustup` tell as itself and that branch requires no rustup. `t-rust-install-export` needs no pixi: cargo-only std/extern-C fixture, find_package chain on the system toolchain. Full e2e suite wall ~6s warm; negative legs assert against the driver's raw `_configure.log`/`_build.log` (2026-09-22, WP3); WP7 re-run 2026-09-22: 54/56 with the two new tool legs contract-skipped, the `t-rust-e2e` pixi keeper PASSED this run (R-12 solved on re-attempt, not chased) |
 cells export `POLYORCH_TEST_GENERATOR` beside `POLYORCH_TEST_CONFIG` so the fixture-driver cases build on the cell's generator+config (2026-09-22, WP3 re-run); WP6 re-run 2026-09-22: the three new cases rode every cell; WP8 re-run 2026-09-22: +installplan rides every cell, +install-export rides the classic cells and joins the MC-cell exclusion list (its fixture-driver artifact contract is single-config shape, like the four pre-WP4 harnesses; matrix.sh `excl` regex updated) -- the sole failure in all five cells being the same R-12 `t-rust-e2e` conda-forge solve state as the e2e row above (not a cell regression) |
| skip contract + self-lock | scratch-copy proofs (repo untouched): canary tripwire fires; executed==0 tripwire fires; unmarked ": SKIP (" vetoes in BOTH drivers; poisoned-HOME run -> 3 contract skips (run.sh skip count) / 3 ctest Skipped, rc=0 | pass (2026-09-22, D15) |
| rust-basic standalone chain | `cmake -S examples/rust-basic -B <b>`, then `--build --target greet-cargo` + `--target run-greet` | configure (system route, no network) -> cargo build -> run-greet prints `hello, world!` (2026-09-22, WP2; cargo from `~/.cargo/bin` on the composed PATH; embedded-host env caveat PIT-14 unchanged -- the wrappers strip compiler-shaping env) |

## Open Items

- [x] **O1 RESOLVED** (2026-09-20, D10) -- validation repository is the sibling monorepo hosting this checkout; identity recorded only in the git-excluded plan zone per C6
- [ ] **O6 REOPENED** (D10) -- the validation host is Linux/x86_64; macOS-only no longer holds, Linux enters scope at first experiment contact
- [x] **O9 RULED** (2026-09-22, D17) -- invariant 2 governs configuration, not artifact bytes; staged copy into standard dirs permitted, uniform path = layout convention, native paths may ride debug metadata; contracts amended same commit; unlocks full-port WP4
- [ ] **Prototype not on this host** (D10.4) -- engine and environment manager are not installed and the prototype tree is absent; the first experiment re-implements the cargo bridge from the contract (plan zone S1-S2)
- [ ] **Experiment-field coverage gaps stay open** (D10.3) -- npm bridge and O4 get zero coverage from the validation target; do not treat them as adjudicated
- [x] **O3 RESOLVED** (2026-09-20) -- project-level addon declaration confirmed: `add_addons("<name> <range>")`, resolved versions pinned in `xmake-addons.lock`, registry at `~/.xmake/addons/addons.conf`; see `decisions.md` D6
- [ ] **O2 -- a decision, not a verification.** The whitepaper's `xmake-idea`/DAP claim found **no support**: zero matches for `xmake-idea`, DAP or `launch.json` across the vendored corpus, whose only VS Code debug path debugs Xmake's own Lua. Remove or annotate the claim in `docs/whitepaper.md`
- [ ] **O4 -- narrowed.** vcpkg/conan **as Xrepo package sources is confirmed** (`xrepo install vcpkg::zlib`, `conan::zlib/1.2.11`); the open half is bringing them **inside a Pixi-managed environment** -- Pixi is mentioned nowhere in the corpus. This is the last violation of the D4 pinning invariant
- [ ] **O5 -- reframed.** `xmake.executable` / `${workspaceFolder}` is documented nowhere; the documented knob for pinning a project-local binary is `XMAKE_PROGRAM_FILE` (plus `XMAKE_PROGRAM_DIR`). Verify against `xmake-vscode` itself, then pick D7's mechanism
- [ ] **Decide the addon-version detection mechanism** for `doctor` -- no runtime version accessor is documented, and the addon version lives in the xmake-repo recipe/tag, not `addon.lua`. Open in `decisions.md` D6 and `docs/modules/03-error-model.md`
- [ ] **Review the whitepaper's unsupported claims** -- Guild (PIT-2) and the CLion/DAP assertion (reinforced by the O2 finding above)
- [ ] **Fill the openspec coverage gap** — PolyOrch ships `apply-change` / `archive-change` / `sync-specs` but no `explore` / `propose` / `verify`. Generate them from the `openspec` CLI, their canonical author, rather than porting a drifted sibling copy; see D9
- [x] **`doc-audit` PORTED** (2026-09-20) — re-authored in English from a sibling repository's copy and rebound to this repository's documents and `C` / `D` / `PIT` numbering; see D9
- [x] Source surface landed: single pixi CMake module + scripts + tests + examples (D12)
- [ ] Resolve the D3 vs reality gap: the in-tree surface is CMake; D3 records the implementation as Lua-in-an-Xmake-addon -- amend the decision or fold the CMake modules into the addon plan (doc-audit question)
- [x] Rust helpers landed: dual-route toolchain + build/test/run/clean wrappers (D13); example `rust-basic` lands with the squad's commits; cross-`--target` routing deliberately out of v0 scope
- [x] **Corrosion gap backlog CLOSED via D14 execution** (`602273f..7ac0c71`): profile<->CMAKE_BUILD_TYPE, ALL/NO_DEFAULT features + RUSTFLAGS/env/cargo-flags setters, --unset host-leak isolation, native-static-libs link interface (+ C-consumer proof), metadata batch import, install+export stubs, PREBUILD seam, three-tier testing (identity pins, rule-wiring, matrix).
- [ ] **Rust deferred register** (post-D14, explicit non-gaps): cross-`--target` routing + hostbuild + cc-rs forwarding **LANDED in WP5**, linker control plane (explicit knob / wrapper / RUNNER / link_libraries) **LANDED in WP5b** -- real-musl legs green; the emulator/THINLTO/OBJC/RC/ASM cluster + Windows/Android derive chains DEFERRED with grep-evidence at the port ledger (mechanism absent from the pinned c4786e7); multi-config output staging / `IMPORTED_LOCATION_<CFG>` cluster **LANDED in WP4** (D17-governed; deviations ledgered); workspace/version/knob cluster (import-time global ALL_FEATURES/NO_DEFAULT_FEATURES/CARGO_FLAGS/VERBOSE/NO_USES_TERMINAL defaults, `PolyOrch_RUST_DEFAULT_KINDS` pair dispatch, `polyorch_rust_package_version` + `POLYORCH_RUST_PKG_<name>_VERSION`) **LANDED in WP6** (stale brief anchors registered in the ledger); install/export **non-root half LANDED in WP8** (full keyword surface through `_polyorch_rust_install_plan`, generated `<export>Config.cmake`, fixture-level find_package chain; stale brief anchors -- COMPONENTS/ARCHIVE_OUTPUT_ARTIFACTS/per-component blocks -- registered as extensions/absent in the ledger) -- still deferred there: MC/per-CONFIG replay-stub staging, file-set/`install(DIRECTORY)` header walks, and the root `PolyOrchConfig.cmake` + PolyOrch-package export chain (BLOCKED-ON-HOST); **WP7 live tool legs pending network + user authorization** (`cargo install cxxbridge-cmd`/`cbindgen` never run: crates-io measured FALSE at WP7 time, shared-state gate demands explicit authorization at first attempt with `cargo uninstall` rollback; the gated e2e cases + bootstrap ALLOW_INSTALL branch + the `cargo tree -i cxx` version derivation all ride that moment; macOS install_name rewrites at install; local (crate-scoped, `cargo rustc`) rustflags; INHERITABLE feature propagation; rustc floor gate LANDED in WP3 (`PolyOrch_RUST_MIN_VERSION`) -- still deferred: a cargo-side floor, ceiling/RANGE-exclude forms, and the reference's cross-toolchain version auto-scan (ruled deviation in the port ledger); `Rust_LLVM_VERSION` parsing (not yet reconciled -- WP10 surface pass); per-crate native-libs variance; ctest-vs-run.sh SKIP asymmetry (G2 finding, drivers left as-is).
- [ ] Gate runner in `scripts/` (canonical C1-C6 + suite one-shot)
- [ ] docs/derived/* + whitepaper-facing prose still describe the xmake-engine/Lua-addon product shape -- sweep them to D16 (CMake-first) at next doc-audit; README/AGENTS/status were aligned 2026-09-22

## History

MediaServo-era memory (through 2026-08) was **removed** from PolyOrch on 2026-09-18 — no in-repo copy is kept. Source of truth remains the `DEVSYS/MediaServo` repository.
