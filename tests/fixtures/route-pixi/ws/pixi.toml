[project]
name = "route-pixi-fixture"
channels = ["conda-forge"]
platforms = ["linux-64", "linux-aarch64", "osx-arm64", "osx-64"]

[environments]
rust = { features = ["rust"], solve-group = "default" }

[feature.rust.dependencies]
rust = "*"
